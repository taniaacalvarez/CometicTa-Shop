name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Instalación de dependencias
        run: echo "Simulación de instalación de dependencias completada ✅"

      - name: Ejecución de pruebas automatizadas
        run: echo "Simulación de pruebas completadas exitosamente ✅"

      - name: Despliegue automático (opcional)
        if: success()
        run: echo "Simulación de despliegue completada 🚀"
